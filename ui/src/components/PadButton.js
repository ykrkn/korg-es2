import React, { Component } from 'react';
import ToggleButton from './ToggleButton';

export default class PadButton extends ToggleButton {
    constructor(props) {
        super(props);
        // this.state = {};
    }
}